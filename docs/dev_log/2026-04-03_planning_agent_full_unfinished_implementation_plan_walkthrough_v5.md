# 2026-04-03 Planning Agent 未完成部分全量实施计划 Walkthrough v5

## 本次做了什么

1. 基于 `master v29` 的真实状态，重写了剩余工作的全量实施合同
2. 把“已经做成”和“还没做成”重新拆清
3. 把剩余工作按 `P1 第一阶段必做 / P2 后续增强` 分层
4. 为每个工作包补了：目标效果、当前差距、代码范围、实施步骤、验收标准、测试集合

## 为什么要做这版

前面的文档和主计划已经能说明方向，但还不够像“执行合同”。

用户这轮明确要求的是：

1. 不再只讲现状
2. 要把未完成部分做成全量实施计划
3. 要写清楚步骤、效果、验收、测试与后续执行方式

所以这版的作用不是再复盘，而是把后续所有批次都绑到一个统一的剩余工作清单上。

## 这版最重要的改写

1. 不再把单条 lane 修绿写成整体完成
2. 明确 `W1-W6` 才是第一阶段真正必须做完的部分
3. `W7-W8` 作为下一层增强，不再和第一阶段完成定义混写
4. 明确下一批就应该先做：`W1` 全量 OpenClaw acceptance，然后进入 `W2` Task Truth Layer 扩面

## 备注

这次是文档冻结，不含产品代码改动。
